.mermaid {
  &.graph svg{
    max-height: 300px;
  }
  @import 'flowchart';
  @import 'sequence';
  @import 'gantt';
  @import 'class';
  @import 'git';
  @import 'pie';
  @import 'state';
}

:root {
  --mermaid-font-family: "trebuchet ms", verdana, arial;
}

