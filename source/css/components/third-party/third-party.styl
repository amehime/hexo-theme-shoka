@import 'pace';
@import 'loading';
@import 'search';


.katex,
.katex-display {
    user-select: all;
}

.medium-zoom-overlay {
  z-index: $zindex-5 - 1;
}

.medium-zoom-image--opened {
  z-index: $zindex-5;
}

.lozaded {
  animation: blur .8s ease-in-out forwards;
}
