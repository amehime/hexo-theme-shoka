.tip {
  position: fixed;
  background: var(--grey-9-a5);
  color: #fff;
  top: 50%;
  left: 50%;
  z-index: $zindex-5;
  padding: 10px;
  border-radius: 10px;
  transform: translate(-50%,-50%);
  text-align: center;
  animation: fadeIn .5s;
}
